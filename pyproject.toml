[project]
name = "football-pred"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "returns>=0.21.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
	"black",
	"mypy",
	"pytest"
]
